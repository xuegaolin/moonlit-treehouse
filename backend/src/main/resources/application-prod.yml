server:
  port: ${PORT:8081}

spring:
  application:
    name: moonlit-treehouse

  datasource:
    url: jdbc:mysql://sh-cynosdbmysql-grp-n2h58owi.sql.tencentcdb.com:29436/treehouse?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true&autoReconnect=true&failOverReadOnly=false&maxReconnects=3
    username: treehouse
    password: ...000xue
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      connection-timeout: 30000
      maximum-pool-size: 10
      minimum-idle: 2
      idle-timeout: 600000
      max-lifetime: 1800000
      connection-test-query: SELECT 1

# CloudBase
cloudbase:
  env-id: moonlit-treehouse-d2d4lt0dd2a525
